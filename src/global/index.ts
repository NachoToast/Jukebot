import { commitHash } from './commitHash';
import { config } from './config';
import { devmode } from './devmode';
import { startTime } from './startTime';
import { version } from './version';

export const JukebotGlobals = {
    commitHash,
    config,
    devmode,
    startTime,
    version,
};
