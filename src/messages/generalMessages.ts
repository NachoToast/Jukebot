export const generalMessages = {
    /**
     * Sent when `/queue` is called when no Jukebox exists, or one does exist but has an empty queue.
     *
     * Also sent when a `/queue` embed is updated via button interactions and the queue is empty.
     */
    emptyQueue: 'Nothing is currently queued',
};
