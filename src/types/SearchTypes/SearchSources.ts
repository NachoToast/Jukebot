export enum SearchSources {
    /** A YouTube link. */
    YouTube,

    /** A Spotify link. */
    Spotify,

    /** A text search. */
    Text,

    /** An unknown URL. */
    Unknown,

    /** An invalid unknown URL. */
    Invalid,
}
